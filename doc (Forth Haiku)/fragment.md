“In a graphics GPU pipeline, rasterization turns each triangle into many fragments, each fragment representing the data for a single pixel (or sample) covered by that triangle.”

In the GPU graphics pipeline, rasterization explicitly converts each triangle (primitive) into a set of fragments—one fragment per covered pixel/sample. Every graphics reference states this exactly: “triangles are rasterized into fragments.”
